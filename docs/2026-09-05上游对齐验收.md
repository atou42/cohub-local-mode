# 2026-09-05 上游对齐验收

## 状态与边界

本次已完成 v2.41 rebase、旧主线历史连接及本地回归修复。当前仍是待验收的集成分支，不能合入 main，也没有部署或重启服务。完整后端构建和计费测试被私有依赖读取权限阻塞；已通过的本地构建和启动检查不能代替这些未通过项。

目标仓库为 `https://github.com/atou42/cohub-local-mode`，上游为 `https://github.com/talesofai/cohub`。锁定 fork main 为 `3a22502372639e764fefe033a71f51b47601f724`，上游 main 为 `a9f2ce352124c46f0ce5c88e9b6596cdf3b60f5a`，共同祖先为 `dbf83380d6bb21cbcd46c56ecfafd7b2d5bda384`。本轮结束前重新读取远端，两端仍为这两个锁定提交。

## 保留证据

64 个 fork 独有提交全部重放，上游 10 个独有提交全部纳入。纯 rebase 结果为 `97613f5cfe8ff568beb3bd815b420fd27727dad9`。`git range-diff dbf83380..3a225023 a9f2ce35..97613f5c` 映射出 61 个完全相同的补丁，另外 3 个只涉及生成 SDK 版本、锁文件依赖上下文和格式。没有丢弃功能提交。9 月 5 日最后 10 个修复全部保持相同补丁。

冲突仅发生在 `apps/api/package.json` 和 `pnpm-lock.yaml`。保留本地 CLI 工作区依赖及 Cloudflare 类型版本，同时采用上游生成 SDK 0.1.25，没有采用整文件 ours/theirs。相对旧 fork 的净变更与上游变更相符，纯 rebase 仅额外有一行包文件格式差异。数据库 schema 和迁移目录相对旧 fork 没有变化。

用户授权后，在任务分支连接旧 main 历史，提交为 `205365406bae7d75d8e3a0151855ac5e1996d759`。连接前后 tree 都是 `2210ae24b273c37e645a9fe2d87f35505b68e12b`。`git merge-tree --write-tree 3a225023 20536540` 成功并返回相同 tree，证明正常合并不会再次应用旧补丁，不需要强制改写 main。上游和旧 fork main 均是结果的祖先。

静态提交映射覆盖 Local Space 生命周期、Mac mini host、Codex/Grok Build/Pi/Cursor、模型与参数选择、Relay v2、跨 Space 文件访问、外部 Agent 会话 fork、iOS/PWA 和移动布局。自动化回归覆盖取消隔离、启动失败清理、前台 stdin 关闭、能力刷新失效、临时会话重试、生成轮询重试、持久化链接、发布失败恢复及 Space 来源异常。此证据证明补丁保留及所测行为，不宣称所有生产场景已端到端通过。

## 本轮修复

真实类型检查暴露三处不兼容。Cloud Space 代理测试补齐上游执行身份新增字段，新增 App Action 无 Session 时返回 403 且不会读取宿主凭证或发请求的断言。Space 合并列表增加 `SpaceRecord` 上下文类型，避免来源被推断为任意字符串。临时会话重试测试将无效的 `grok` 名称改为协议中的 `grok_build`，同时覆盖 Pi。没有删除原测试或修改业务失败处理。

唯一生产源码改动是列表的类型标注。用仓库已安装的 esbuild 分别编译修改前后文件，断言输出 JavaScript 完全一致。因此此前完成的 Local Mode 构建与修复后运行代码等价；其他改动仅为测试和本文档。

## 实际验证

使用 Node 25.3.0、pnpm 10.32.1，独立 HOME、临时目录、数据目录和测试配置。`pnpm install --frozen-lockfile --ignore-scripts` 成功。生命周期脚本没有运行，锁文件未因安装变化。测试不读取生产凭证或数据。

`pnpm lint` 通过；最终三处修改另运行 `pnpm exec biome lint` 通过。`pnpm --filter web typecheck` 最终串行运行得到 0 errors、0 warnings。一次较早检查与 Web 构建同时运行，产生临时输出目录 ENOENT，虽然工具退出 0 也没有计为通过；构建结束后串行重跑才通过。额外的 `biome check` 发现两个文件已有格式差异，不属于仓库 lint 失败，也没有为此扩大格式重写范围。

构建 protocol、SDK 及所需内部依赖后，各工作区测试分批执行。最终结果为 protocol 86、relay 76、infra 26、gateway 18、CLI 77、sandbox-client 4、controller 2、core 95、worker 52、SDK 288、agent 116、API 166、Web 532 个测试通过，共 1538 个独立 JS/TS 测试。API 166 的依据是完整 165 个通过后，新增代理失败路径测试单独执行 7 个通过，其中 6 个为既有用例，不重复计数。Web 532 为修复后的完整套件。

`node --test scripts/local-mode/*.test.mjs` 初次有 78 个通过，一个文件因 protocol 未构建而不能加载；构建后重跑该文件 3 个通过，合计 81 个独立 Local Mode 脚本测试。`go -C apps/sandbox test ./...` 通过。首次 SDK 测试因缺少已构建分发文件和测试环境的 ENV 选择失败，构建 SDK 并修正隔离环境后完整 288 个通过，未修改 SDK 业务代码。

`pnpm local:build` 通过，生成真实 Sandbox 二进制和完整 Cloudflare Web Worker、静态资源，并通过现有原子发布产物检查。首轮隔离配置缺少构建所需 `PUBLIC_MARKETPLACE_APP_ID`，补齐空的可选应用 ID 后通过；没有配置虚构市场应用，也没有部署。

使用可丢弃 Redis、独立数据目录和回环地址真实启动 Local API。`/readyz` 返回 ok，`/healthz` 返回 Redis ready，Local Mode route-health 标识正确。非法 Host 返回 403，未认证 App Action 返回 401。首次测试探针使用 fetch 手工 Host 未实际发送该头，改用 node:http 后真实拒绝路径通过，未为此改产品代码。

真实构建的 Web Worker 通过 Wrangler 本地模式启动。共享浏览器中，Local Mode 连接页在 1440×900 和 390×844 都正常显示，文案无重叠、无水平溢出，脚本、样式、manifest 与恢复脚本响应 200。未连接云端账户状态明确显示，没有用假登录或模拟页面冒充已登录工作区验收。真实云端会话、付费调用及完整已登录聊天和内联图片预览仍未验证。

## 阻塞与合并门槛

锁文件中的 `@talesofai-billing/sdk@1.16.0` 来自 `git.talesofai.com` 私有包仓库。对锁定 tarball 的实际 GET 返回 HTTP 401，本机没有 `GITEA_NPM_TOKEN`，共享浏览器该站点也未登录。可选安装允许其缺席，但不能因此声称完整构建可用。

后端完整构建、涉及 billing 的类型检查和 billing 测试仍失败，最终 API 类型错误仅剩这个缺失依赖。没有用 mock 包、修改模块声明、删测试或吞错掩盖。补齐合法包读取权限后，必须重新冻结安装，再运行完整构建、类型检查、billing 及相关 API 回归；有依赖能力的 CI 可以提供这部分证据，但必须检查真实结果。

fork 中继承的主线部署工作流仍为 active，包含 main push 后 Web、API、Agent、Gateway、Worker 的发布逻辑。未检查或改动任何 secret 值。本轮没有部署授权，合并前还必须确认不会触发未授权发布；不能以工作流可能没有凭证为理由忽略此边界。PR 不添加 preview 标签，预览发布有该标签的人为门槛。

只有依赖阻塞解除、必要检查全部通过、已登录受影响路径验收完成、自动发布边界得到处理之后，才能将 PR 转为 ready 并合入 fork main。本轮保持 Draft，不合入、不强推、不部署。

## 恢复与复核

旧 fork 完整提交 `3a22502372639e764fefe033a71f51b47601f724` 是不可变恢复点，且仍在结果历史中。本次所有操作在独立 clone，未改动其他工作树。合并前可关闭 Draft PR 并保留分支作调查，不需要回退 main。将来若正常 merge 后需要撤回，应基于实际 merge commit 另建 revert PR，不强制重置公共主线，也不自动回滚生产服务。

逐提交映射可由上文固定 SHA 的 `git range-diff` 重现。下方保留全部 64 项索引，等号表示补丁相同，感叹号的三项差异已在上文解释。

```text
01 cf571740 ! ac6c3439 Mac mini local mode
02 fe4ad2a4 = accf2746 compiled host mode
03 3153fd97 = 000e0eea macOS persistence
04 1050c491 = 2bb5fd14 protected local mode
05 e8cdb11c = ac9b8082 harness-aware routing
06 2934d25b = b8f2fc56 Tailscale routing
07 bc3facea = 8915f519 sandbox autostart
08 ec786454 = 9d9bc2ab composer isolation
09 baf6d4e4 = b2f31e85 relay/runtime integration
10 803ccfe8 = 9ca8588b relay protocol v2
11 21190391 = afdcbc9c protocol ledger
12 65144e42 = dcffd894 rollout ledger
13 eda13864 = e673c695 prior upstream integration
14 a829d2ea = 58cf532f upstream monitoring
15 e0ef66fb = 6eeaaed1 monitoring ledger
16 fdaa4ecf = bd21d3be atomic web releases
17 e3b37981 = 39182bd4 ultra effort
18 28a25c47 = 5932c9e8 root ingress
19 c4dedf6b = 02a5a803 model catalog refresh
20 599deeac ! 95192723 agent/cloud generation capabilities
21 ae96a519 = d347f6a7 Cursor efforts
22 1512aeef = d92d3268 rebase publication ledger
23 10e8c377 = 89220e0c iOS safe-area theme
24 fc98a550 = 92638714 mobile full-bleed chrome
25 9730ab8f = 69a514d4 iOS viewport
26 d418d084 = f09852e8 iOS ledger
27 bdfaf6d3 = d95ecf7d mobile metadata
28 b5720969 = dc102d5a metadata ledger
29 0a407806 = a7758aa4 compact controls
30 51769db0 = eb584f1c controls ledger
31 dab84109 = 13bc2b0d host scope
32 c07cc713 = 7dbe51c3 agent controls
33 b79b3308 = 8302fdb6 persistent client recovery
34 6d6e323e = f78c77a8 integration ledger
35 2d6e0d14 = 0e3d47db integration closure
36 15997e93 = 7787add7 external session fork
37 78c41c39 = b9559fee iOS focus board
38 cbc0cd13 = 94a1ccda September ledger
39 696cad2b = 6c576d4c split deployment prevention
40 d88c8e35 = 745c98ad relay release identity
41 bcd943be = 187915b7 federated spaces/harness continuity
42 4aa2ea44 = 3da09091 September 3 ledger
43 4b85e133 = 55b7ec92 v2.38 build integration
44 3f9e8d3f = eec0928d Cursor ACP compatibility
45 dd002cab = 72cf0bf5 upstream test contracts
46 f46dd46d = d4d98579 turn history
47 ba8b49c6 = 9ed42118 v2.39 ledger
48 e841c1ee = 5a53dd24 stale-client release prevention
49 8ec2232b = f1b9dcd6 white-screen ledger
50 93e4a60f = ef398490 September 4 ledger
51 81d2762d ! 1ad78464 v2.40 integration
52 52b5cc82 = adb2327c v2.40 verification
53 6152ca77 = 260d295d host release worktree
54 e280b903 = 52304cf0 v2.40 release ledger
55 4990b759 = 6af5e757 Codex interrupt isolation
56 99be7c03 = a1612cf4 partial Space source failures
57 520cb38d = b73799f8 ACP failed runtime cleanup
58 50152f97 = 463011c0 foreground bash stdin
59 958b1441 = 29f1fd31 obsolete capabilities invalidation
60 7bcd15ae = 3612881a provisional relay retries
61 a567ed02 = 3c2756a4 transient generation polling retry
62 dd700ca9 = 347ff6dc durable artifact links
63 578a011c = 4a5bcada failed Worker release recovery
64 3a225023 = 97613f5c recent local Space picker
```
