/** Local Relay wire versions shared by every producer and consumer. */
export const LOCAL_RELAY_NODE_PROTOCOL_VERSION = 3 as const;
export const LOCAL_RELAY_BROWSER_PROTOCOL_VERSION = 2 as const;
export const LOCAL_RELAY_EVENT_SCHEMA_VERSION = 1 as const;
